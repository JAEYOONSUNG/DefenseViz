% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_rebase_comparison.R
\name{get_rebase_data}
\alias{get_rebase_data}
\title{Get REBASE Data as R Table}
\usage{
get_rebase_data(force_download = FALSE, verbose = TRUE)
}
\arguments{
\item{force_download}{Force re-download even if cache exists}

\item{verbose}{Print status messages}
}
\value{
Tibble with columns: enzyme_name, enz_type, rec_seq (recognition sequence),
org_name, sequence (protein), seq_length, rm_type, subunit
}
\description{
Downloads (if needed) and returns REBASE gold standard data as a tibble.
Contains enzyme names, recognition sequences, protein sequences, and type classifications.

REBASE updates on the 1st of each month. This function:
\itemize{
\item Downloads fresh data if no local cache exists
\item Re-downloads if local cache is from a previous month
\item Uses cached data if it's from the current month
}
}
\examples{
\dontrun{
# Get REBASE data table
rebase <- get_rebase_data()

# Filter for Type I enzymes with recognition sequences
type_i <- rebase \%>\%
  filter(rm_type == "Type_I", rec_seq != "")

# Get unique recognition sequences
rec_seqs <- rebase \%>\%
  filter(rec_seq != "") \%>\%
  distinct(enzyme_name, rec_seq, rm_type)
}
}
